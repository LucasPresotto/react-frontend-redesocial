1. Clone o repositório:
    ```bash
    git clone https://github.com/LucasPresotto/react-frontend-redesocial.git
    ```

2. Entre na pasta:
    ```bash
    cd react-frontend-redesocial
    ```

3. Utilize o comando para baixar os pacotes do composer.json:
    ```bash
    npm update --save
    ```

4. Rode o projeto em modo de desenvolvimento
    ```bash
    npm run dev
    ```